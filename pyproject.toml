[project]
name = "aadithya-quantlab"
version = "0.1.0"
description = "Structured intraday NIFTY quantitative research laboratory."
requires-python = ">=3.10"
dependencies = [
  "numpy>=1.24",
  "pandas>=2.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

